package legacy.map.fixtures;

import legacy.map.TileFixture;
import legacy.map.HasKind;

/**
 * An (at present marker) interface for tile fixtures representing some kind of
 * "mineral" (as opposed to organic) resource or thing: bare rock or the
 * ground, stone deposit, mineral vein, mine, etc.
 *
 * TODO: Any members?
 */
public interface MineralFixture extends TileFixture, HasKind {
}
