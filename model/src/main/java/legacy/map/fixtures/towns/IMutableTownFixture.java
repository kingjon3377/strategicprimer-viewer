package legacy.map.fixtures.towns;

import legacy.map.HasMutableOwner;
import legacy.map.HasMutablePortrait;

public interface IMutableTownFixture extends ITownFixture, HasMutableOwner, HasMutablePortrait {
}
