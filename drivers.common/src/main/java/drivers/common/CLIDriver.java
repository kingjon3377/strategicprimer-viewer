package drivers.common;

/**
 * An interface for drivers which operate on a map model of some kind and
 * should have maps written back to file when they exit.
 */
public interface CLIDriver extends ModelDriver {
}
